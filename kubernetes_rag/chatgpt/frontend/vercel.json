{
  "buildCommand": null,
  "outputDirectory": ".",
  "framework": null,
  "rewrites": [
    { "source": "/api/:path*", "destination": "https://$PUBLIC_BACKEND_HOST/api/:path*" },
    { "source": "/ws/:path*", "destination": "wss://$PUBLIC_BACKEND_HOST/ws/:path*" }
  ],
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        { "key": "X-Frame-Options", "value": "DENY" },
        { "key": "X-Content-Type-Options", "value": "nosniff" }
      ]
    }
  ]
}
